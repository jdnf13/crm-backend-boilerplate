------------------------------------------------------------------
Configuración del Servidor Express
------------------------------------------------------------------
PORT=4000
NODE_ENV=development

------------------------------------------------------------------
Configuración JWT (Autenticación)
------------------------------------------------------------------
CLAVE SECRETA: Genera una cadena aleatoria larga para producción.
JWT_SECRET=super-secreto-de-autenticacion-para-el-crm
JWT_EXPIRES_IN=7d

------------------------------------------------------------------
Configuración de la Base de Datos (MySQL - Docker)
------------------------------------------------------------------
Las variables aquí deben coincidir con lo que usa el docker-compose.yaml
DB_HOST debe ser el nombre del servicio (db) dentro de la red Docker.
DB_HOST=db
DB_PORT=3306
DB_USERNAME=crm_user
DB_PASSWORD=crm_password123
DB_NAME=crm_db
DB_ROOT_PASSWORD=crm_root_password123